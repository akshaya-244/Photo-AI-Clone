/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['fal.ai', 'v3.fal.media'], // Add fal.ai to the list of allowed domains
    },
  };
export default nextConfig;
